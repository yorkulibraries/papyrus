// If coming from renewal form, remove and d-noneeverything
$("#renew_item_access_form").modal("d-none");
$("#renew_access_form_holder").remove();

<% @items.each do |item| %>  
  $("#item_<%= item.id %>").replaceWith('<%= j render(item, show: "li") %>');
<% end %>
