// If coming from renewal form, remove and hide everything
$("#renew_item_access_form").modal("hide");
$("#renew_access_form_holder").remove();

<% @items.each do |item| %>  
  $("#item_<%= item.id %>").replaceWith('<%= j render(item, show: "li") %>');
<% end %>
