$("#access_code_form_holder").empty();
$('#access_code_form_holder').prepend('<%=escape_javascript render("shared_access_codes/form") %>');

$( ".datepicker" ).datepicker({ dateFormat: 'yy-mm-dd' });
