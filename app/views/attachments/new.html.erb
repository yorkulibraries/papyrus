<% title "Upload A File", false %>

<div class="row">
	<div class="col-md-12">
		<%= panel(title: "Upload New File for Item: #{@item.title} ", icon: "file") do %>
		<%= link_to item_path(@item), class: "btn btn-primary btn-sm" do %> &laquo; Back to item details <% end %>
		<hr/>

			<%= render 'form' %>
		<% end %>
	</div>

</div>
