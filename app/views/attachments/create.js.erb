$("#announcement_form_holder").empty();
<% if @announcement.errors.size == 0 %>
  $("#uploaded_attachments").append('<%=escape_javascript render @attachment %>')
  $(".blank-slate").hide();
<% else %>
   alert("Failed to upload file: <%= j @attachment.errors.full_messages.join(', ').html_safe %>");
<% end %>
